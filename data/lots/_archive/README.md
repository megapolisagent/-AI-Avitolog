# Архив лотов

Сюда переезжает файл карточки лота из `data/lots/` при снятии с публикации, замене условий или продаже — не удаляется совсем.

Зачем: реальный инцидент 2026-09-10 (`.claude/rules/ask-before-searching.md`) — агент, не найдя активный лот в `data/lots/`, реконструировал параметры через `git log`. Правильное поведение — проверить эту папку (штатное designated input location для снятых лотов) и, если и здесь пусто, спросить владелицу напрямую, не искать по истории git.
