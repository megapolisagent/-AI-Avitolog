# Дневник 2026-09-14

## Найдено и устранено самопротиворечие во Входном гейте (§4), напрямую конфликтовавшее с уже верным §3

### Что сделали
- По итогам внешнего разбора экосистемы (сессия владельца в другом агенте) найдено: `CLAUDE.md` §4 «Входной гейт задачи» требовал подтверждения на **любое** исследование/создание «без исключений по осям» — но §3 «Автономия» этого же файла явно называет разведку рынка и упаковку карточки автономными, без вопроса каждый раз. Один устав одновременно требовал и не требовал подтверждения на одно и то же действие.
- Тот же баг обнаружен в `ENGINEER/CLAUDE.md` §3 и `AI Marketing Strategist/CLAUDE.md` §7 — источник во всех трёх один: `FOUNDATION/instructions/confirmation-before-action.md`, добавленный 2026-09-14 и слитый при переносе с осевым гейтом `HOME.md`.
- Исправлено: §4 переписан так, чтобы Confirmation Gate был формализацией уже существующей границы риска из §3 (публикация/цена/платное продвижение — подтверждение; разведка/упаковка/расчёт — автономно), а не обобщённым «любое исследование».
- Консолидированы 4 файла правил (`ask-before-searching.md`, `direct-reading-policy.md`, `direct-response-policy.md`, `verify-before-analyze.md`) в один `data-discipline.md` — та же консолидация, что у `ENGINEER` и `AI Marketing Strategist`. Обновлена единственная найденная ссылка на старое имя в `avito-listing-packaging/SKILL.md`.
- Перенесены и подключены в `.claude/settings.json` (новый файл, раньше не существовал) два хука от ENGINEER: `guard-rules-bloat.sh` (потолок 9 файлов правил + запрет инцидент-нарратива в новых) и `block-dangerous-git.sh`. Добавлен `permissions.ask: ["Agent"]` — запуск любого субагента (`market-analyst`, `pain-point-extractor`, `ad-strategist`, `traffic-manager`, `reviewer`) теперь технически требует подтверждения, не только по самооценке модели.

### Что осталось (обновлено — сделано в этом же ходу)
- Старые 4 файла правил удалены с подтверждения владельца.
- Прочитан `avito_api_client.py` целиком: реального «publish/change price» вызова в этом файле нет — `scripts/generate_feed.py` честно признаётся, что фид никуда не публикует (хостинг ещё не выбран), так что текущий реальный риск — ровно 7 явно помеченных «МУТИРУЮЩИЙ» методов (`answer_review`, `delete_review_answer`, `create_offers`, `confirm_offers`, `create_bbip_order`, `apply_trxpromo`, `cancel_trxpromo`).
- Построен и подключён `.claude/hooks/guard-avito-mutations.sh` — матчится на вызов любого из этих 7 методов и в Bash/PowerShell-команде, и в контенте, записываемом через Write/Edit (закрывает обход «написать скрипт отдельным шагом, потом запустить»). Вместо жёсткого блока — `permissionDecision: "ask"`: харнесс сам покажет реальный permission-prompt в момент вызова, агент не может тихо ответить за владелицу. Протестировано тремя pipe-тестами (мутирующий вызов в команде → ask; read-only вызов → тихо проходит; мутирующий метод в записываемом файле → ask) — все три сработали как задумано.
- Когда `generate_feed.py` научится реально публиковать фид (хостинг появится) — этот хук нужно будет расширить под тот механизм тоже, он его пока не покрывает (не было что покрывать).

### Что сделали (продолжение — сокращение ритуала без доказанной пользы)
- `capability-resolver.md` — снят протокол внешней разведки (3 источника + Platform Decision Record), физически невыполним этим агентом. Оставлена одна строка «назови потребность владелице, дальше — Engineer».
- `memory-rules.md` — дневник по факту (решение/правка/результат), не по календарю.
- `CLAUDE.md` §4 п.3 — письменный `pre-task-check.md` больше не обязателен для обычной высокой оси, держится в голове; полный план всё равно требуется на Confirmation Gate, дублировать не нужно.

### Решения
- Confirmation Gate этого агента = граница риска из §3, не абстрактное «любое исследование». Решение владельца, 2026-09-14 (см. `FOUNDATION/DECISIONS.md`).
- Ритуалы без доказанной пользы (внешняя разведка, обязательный ежедневный дневник, дублирующий письменный чек-лист) — вырезаны тем же решением владельца, 2026-09-14.

## Довершено (Engineer, тем же днём) — `avito-listing-packaging` разбит по Правилу 3 (Composability Gate baseline)

### Что сделали
- Composability-чекер (`.claude/tools/check_skill_composability.py`) флагнул `avito-listing-packaging` как составной — «Модуль 2 — Медиаплан продвижения» и «Модуль 5 — Разведка рынка» остались внутри общего файла, хотя Модули 1/3/4 уже были разбиты 2026-09-10 по тому же критерию.
- Разбито на два новых фокус-скилла тем же методом: `.claude/skills/media-plan/SKILL.md` (пул карточек, платные форматы, вызов `traffic-manager`) и `.claude/skills/market-recon/SKILL.md` (market-analyst → market_snapshot_analyzer.py → pain-point-extractor, аудит по 6 пунктам, рекомендация). Содержание перенесено без изменений, только формат и разбиение.
- `references/market-raw-data-regulation.md` переехал вместе с Модулем 5 в `market-recon/references/` — обновлена единственная внешняя ссылка на него (`.claude/agents/market-analyst.md`).
- `avito-listing-packaging/SKILL.md` — Модуль 2/5 заменены короткими указателями на новые скиллы, как уже было сделано для Модуля 1. Единственная внутренняя ссылка «если Модуль 5 уже запускался» переименована в `market-recon`.
- Проверено: `module_headers: 0` у `avito-listing-packaging` после правки (было 2) — Rule 3 больше не флагует. Composability baseline пересчитан на всех 10 скиллах.

### Решения
- Разбиение выполнено по явному «да» владельца по этому репозиторию отдельно, 2026-09-14 (Confirmation Gate).
